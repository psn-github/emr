name: CI

# Build + test on GitHub's runners on every push and PR.
# No PHI ever touches this pipeline — it runs against synthetic seed data only.
# This is the "build from GitHub" piece: your laptop is not involved.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    # A throwaway Postgres for integration tests. Synthetic data only.
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: oxford
          POSTGRES_PASSWORD: oxford
          POSTGRES_DB: oxford_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://oxford:oxford@localhost:5432/oxford_test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # --- The steps below activate once application code exists. ---
      # Until apps/ and packages/ are scaffolded by Claude Code in Phase 0,
      # this workflow succeeds as a no-op so the pipeline is green from day one.

      - name: Detect project
        id: detect
        run: |
          if [ -f package.json ]; then
            echo "has_app=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_app=false" >> "$GITHUB_OUTPUT"
            echo "No application code yet (spec-only repo). CI is a no-op until Phase 0 scaffolds the monorepo."
          fi

      - name: Setup pnpm
        if: steps.detect.outputs.has_app == 'true'
        # Version comes from package.json "packageManager" (pnpm@9.15.9); do not
        # also pin it here — action-setup@v4 errors on a double-specified version.
        uses: pnpm/action-setup@v4

      - name: Setup Node
        if: steps.detect.outputs.has_app == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm -r typecheck

      - name: Lint
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm -r lint

      - name: Module boundaries
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm boundaries

      - name: Migration safety (block destructive migrations)
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm check-migrations-safe

      - name: Cutover invariants (no-cash / no-tax structural rules)
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm check-cutover-invariants

      - name: Secret scan
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm secret-scan

      - name: Unit + integration tests
        if: steps.detect.outputs.has_app == 'true'
        # Serialize packages: integration tests share the one Postgres service,
        # so run them one package at a time to avoid cross-package DB races.
        run: pnpm -r --workspace-concurrency=1 test

      - name: Build
        if: steps.detect.outputs.has_app == 'true'
        run: pnpm -r build
